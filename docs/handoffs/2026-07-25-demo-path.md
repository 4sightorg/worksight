# HANDOFF — E2E demo path (#18)

**Status:** Done (MVP slice)  
**Branch:** `feat/mvp-demo-18` (stacked on `feat/mvp-wire-api-17` + merge of
`feat/mvp-wire-web`)  
**Issue(s):** #18  
**Last updated:** 2026-07-25

## Bottom line

One documented happy path: start API + web → open `/demo` → see common-driven
employee/team/task + well-being widgets. Fixture-backed only (not Supabase).

## Current state

- `docs/mvp/DEMO.md` runbook + `scripts/demo.sh` / `pnpm demo`
- Public `/demo` page fetches Nest `GET /users|/teams|/tasks|/tasks/stats/:id`
- `NEXT_PUBLIC_USE_API=true` wires `/admin/users`, `/tasks`, `/dashboard/tasks`
  to Nest (falls back to local fixtures on API failure)
- API defaults to port **3001** with CORS for the web origin
- Stack depends on sibling #16 (web) and #17 (API) tips

## Hook points

- `apps/web/src/app/demo/page.tsx`
- `apps/web/src/lib/worksight-api.ts`, `mvp-api-bridge.ts`
- `scripts/demo.sh`, `docs/mvp/DEMO.md`

## How to verify

```bash
pnpm --filter @worksight/common build
pnpm --filter @worksight/api build
pnpm --filter @worksight/web build
PORT=3001 node apps/api/dist/main.js &
curl -s localhost:3001/users | head -c 200
# then: pnpm demo   OR open http://localhost:3000/demo with API up
```

## Done means

- [x] Runbook exists and works on this machine
- [x] Curl checklist documents API contract proof
- [x] Burnout / well-being framing visible with fixture data (`/demo`)

## Known gaps

- Authenticated dashboard home still uses localStorage wellness history; `/demo`
  is the honest E2E proof for API ↔ web agreement.
- No screenshot automation in CI yet.
